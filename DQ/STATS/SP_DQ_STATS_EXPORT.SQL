AS
BEGIN
SET NOCOUNT ON;

SELECT ID,TYPE_STAT, REPLACE(STR(VALEUR,10,2),',','.') AS VALEUR, SOCIETE,convert(varchar, DATE_CALCUL, 23) AS DATE_CALCUL,convert(varchar, DATE_SUPPRESSION, 23) AS DATE_SUPPRESSION

FROM STATS_SOCIETES WHERE DATE_CALCUL > GETDATE()-7;

END